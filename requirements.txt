# The Engineering Atlas — pipeline dependencies
# venv:  python3 -m venv .venv && source .venv/bin/activate && pip install -r requirements.txt
# System dependency (NOT pip): ffmpeg  →  macOS: brew install ffmpeg
moviepy==1.0.3       # video_assembler.py
numpy                # video_assembler.py (Ken Burns transforms)
pillow               # text overlays + image handling
google-genai         # generate_images.py / generate_asset.py — Gemini images
# Animation is hand-built in After Effects (docs/after_effects_workflow.md) —
# no generative-video deps needed.
pyyaml               # scripts/fetch_outliers.py — competitor channel list
# anthropic          # optional: scripts/tag_outliers.py (formula tagging) — install if you use it
